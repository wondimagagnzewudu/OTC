import React from 'react';
import PropTypes from 'prop-types';
import classNames from 'classnames';
import { SectionProps } from '../../utils/SectionProps';
import Input from '../elements/Input';

const propTypes = {
  ...SectionProps.types,
  split: PropTypes.bool
}

const defaultProps = {
  ...SectionProps.defaults,
  split: false
}

const Cta = ({
  className,
  topOuterDivider,
  bottomOuterDivider,
  topDivider,
  bottomDivider,
  hasBgColor,
  invertColor,
  split,
  ...props
}) => {

  const outerClasses = classNames(
    'cta section center-content-mobile reveal-from-bottom',
    topOuterDivider && 'has-top-divider',
    bottomOuterDivider && 'has-bottom-divider',
    hasBgColor && 'has-bg-color',
    invertColor && 'invert-color',
    className
  );

  const innerClasses = classNames(
    'cta-inner section-inner',
    topDivider && 'has-top-divider',
    bottomDivider && 'has-bottom-divider',
    split && 'cta-split'
  );  

  return (
    <section
      {...props}
      className={outerClasses}
      style={{ backgroundColor: 'white'}}
    >
      <div style={{ backgroundColor: 'white'}}>
        <div
          className={innerClasses}
          style={{ backgroundColor: 'white'}}
        >
          <div className="cta-slogan">
            <h3 className="m-0" style={{color: '#F24140'}}>
              Subscribe for more up to date information?
              </h3>
          </div>
          <div className="cta-action">
            <Input id="newsletter" type="email" label="Email" labelHidden hasIcon="right" placeholder="Email" style={{backgroundColor: '#e4251824', borderRadius: 20, width: '50%', color: 'white'}}>
            </Input>
          </div>
        </div>
      </div>
    </section>
  );
}

Cta.propTypes = propTypes;
Cta.defaultProps = defaultProps;

export default Cta;